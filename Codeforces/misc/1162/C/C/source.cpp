#include<cstdio>
#include<cmath>
#include<vector>
#include<algorithm>
#include<string>

using namespace std;

int main()
{
	int n, k;
	int *dp[2];
	int i, j;

	scanf("%d %d", &n, &k);

	dp[0] = new int[n + 1];
	dp[1] = new int[n + 1];

	for (i = 0; i < n; i++)
	{

	}

	return 0;
}