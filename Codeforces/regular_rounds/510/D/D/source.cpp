#include<cstdio>
#include<vector>
#include<algorithm>

using namespace std;

int main()
{
	long long n, t, sum = 0;
	long long i, j, input;
	vector<long long> pos, neg, tot;
	int zero = 0;

	scanf("%I64d", &n, &t);

	for (i = 0; i < n; i++)
	{
		scanf("%I64d", &input);

		sum += input;

		tot.push_back()
	}
}